# Remove or comment out this line
# from ..main_api import app

# Instead, you can initialize your own router if needed
from fastapi import APIRouter

# Create a router to be used by other modules
router = APIRouter() 